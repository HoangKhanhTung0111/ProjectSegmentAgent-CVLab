Run "python main.py", it will use the image in the ./Mock_pipeline/inputs

And the mock_model.py will segment the red object in the image (change the "red" in mock_model.py to change the colour of the object to be semented)